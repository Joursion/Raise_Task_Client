/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View
} from 'react-native';

import raise_tasks from './app/'

AppRegistry.registerComponent('raise_tasks', () => raise_tasks);
